version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install --no-cache-dir -r requirements.txt
run:
  command: python3 main.py
  network:
    port: 8080
  env:
    - name: FLASK_ENV
      value: "production"
